% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ches_weights.R
\name{ches_weights}
\alias{ches_weights}
\title{Create balanced weights for CHES expert level dataset}
\usage{
ches_weights(data, country = "country", party = "party")
}
\arguments{
\item{data}{A data frame containing country and party columns from the CHES dataset}

\item{country}{The name of the column containing country identifiers (default: "country")}

\item{party}{The name of the column containing party identifiers (default: "party")}
}
\value{
A data frame with additional columns:
\item{party_obs}{Number of observations per party within each country}
\item{weight}{Calculated weight for each observation}
}
\description{
This function creates weights for a CHES (Chapel Hill Expert Survey) dataset to ensure
each country-party combination has equal influence in subsequent analyses.
}
\examples{
\dontrun{
ches_data <- read.csv("ches_data.csv")
weighted_data <- ches_weights(ches_data)

# With custom column names
weighted_data <- ches_weights(ches_data, country = "nation", party = "political_party")
}

}
